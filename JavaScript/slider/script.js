// const a = document.querySelectorAll('.slider');
// var counter = 0;

// a.forEach(
//     (slider,index) =>
//     {
//         slider.computedStyleMap.left = `${index *100}%`
//     }
// )


// const sliders = document.getElementsByClassName('slider');
// let counter = 0;

// const nextSlide = () => {
//   counter = (counter + 1) % sliders.length;
//   updateSliderPositions();
// };

// const prevSlide = () => {
//   counter = (counter - 1 + sliders.length) % sliders.length;
//   updateSliderPositions();
// };

// const updateSliderPositions = () => {
//   for (let i = 0; i < sliders.length; i++) {
//     sliders[i].style.left = `${i - counter}00`;
//   }
// };

// document.querySelector('.prev').addEventListener('click', prevSlide);
// document.querySelector('.nxt').addEventListener('click', nextSlide);

// updateSliderPositions(); // Set initial positions



// const a= document.querySelectorAll('.slider');
// var counter=0;
// // console.log(a);
// a.forEach(
//     (slider,index) =>
//     {
//         slider.style.left= ${index *100}%
//     }
// )
// const sliderrun=()=>
// {
//     a.forEach(
//         (slider)=>
//         slider.style.transform = translateX(-${counter *100}%
//     )
// }