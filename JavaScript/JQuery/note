$("*")                      -> for universal
$("ul li")
$(".abc, .xyz")      -> access abc and xyz class
$("p.first")           -> select first paragraph
$("p.last")         -> select last para
$("li:even")


Task:
    1. create a table in which datesheet of midterm will be displayed, you have to apply 
    2. demontraste the uses of 15 selectors apply css background color, boarder, properties on the 15 selectors
    3. add one img when you apply onmouse enter event enter inside the img then image will be hide(use .hide)